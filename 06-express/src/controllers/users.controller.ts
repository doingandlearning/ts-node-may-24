import { Request, Response, NextFunction } from "express";

export function createUser(req: Request, res: Response, next: NextFunction) {
  res.send("CreateUser not implemented yet");
}

// export default { createUser };
